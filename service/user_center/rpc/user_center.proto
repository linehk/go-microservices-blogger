syntax = "proto3";

package user_center;
option go_package="./user_center";

message Request {
  string ping = 1;
}

message Response {
  string pong = 1;
}

service User_center {
  rpc Ping(Request) returns(Response);
}
