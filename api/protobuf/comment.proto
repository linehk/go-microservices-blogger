syntax = "proto3";

package comment;
option go_package="./comment";

message Request {
  string ping = 1;
}

message Response {
  string pong = 1;
}

service Comment {
  rpc Ping(Request) returns(Response);
}
