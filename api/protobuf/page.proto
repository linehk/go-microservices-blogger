syntax = "proto3";

package page;
option go_package="./page";

message Request {
  string ping = 1;
}

message Response {
  string pong = 1;
}

service Page {
  rpc Ping(Request) returns(Response);
}
